# 更新日志

## 0.9.7 — 2026-09-19

- **应用图标**：嵌入 Windows exe 图标、窗口标题栏图标；安装后桌面/开始菜单快捷方式使用同款图标，不再是空白默认图标。
- **授权失效及时踢出**：运行中每 2 秒校验本地票据，每 60 秒后台在线复核；卡密过期/冻结后自动回到激活页，无法继续使用主功能。

## 0.9.6 — 2026-09-19

- **总览页布局对齐「清理助手」式信息架构**：
  - 去掉挤在一起的 KPI 四卡与快捷操作条，顶部只保留「共发现 … 可清理」+「刷新扫描 / 一键清理」；
  - 磁盘改为**整行卡片**：圆环 + 进度条 + 已用/可用/总容量同一行对齐，扫描/极速/选为路径放到卡片底栏，避免窄窗口里按钮叠在进度条旁、点不到；
  - 「可清理项目」放在磁盘下方，底栏保留安全提示与「查看扫描详情」。

## 0.9.5 — 2026-09-19

- **布局随窗口自适应**（修「按钮点不到 / 必须把窗口拖宽」）：
  - 磁盘卡片修复列宽计算错误：原实现里第二张卡的宽度被算成「整个可用宽度」（`/(2 - j)`，j=1 时除数为 1），
    导致右侧磁盘**横向溢出到窗口外**，纵向滚动也看不到，只能把窗口拖宽；现在按可用宽度直接算列数
    （每列最少 230pt，最多 3 列），并扣除卡片内边距（16pt×2）与 egui 的 `item_spacing`，
    窄窗口自动退化为单列而不是溢出；
  - 卡片内容限宽 + 图例可换行，避免长文本把卡片撑宽；
  - 总览页**整页**纳入滚动区（原来只有磁盘列表可滚，上半部分固定，窗口一矮就把磁盘区挤没）；
  - 顶部路径栏、KPI 卡行、快捷操作卡、磁盘区标题行、软件卸载/开机自启操作行统一改为可换行布局；
  - 侧栏加滚动区；底部状态/日志区高度自适应、日志默认折叠；最小窗口尺寸放宽到 800×520。
- **彻底移除旧第三方动态域名**：客户端只认 `https://jiaoben.lidashuai.top`，不再识别/重写旧动态域名；
  服务端 nginx 中该域名的 `server_name` 也已移除（仅保留直连 IP 作为备用通道）。
  旧域名请求不再被重写，若某台机器设置里残留旧地址，清空「更新地址」即可回落默认域名。

## 0.9.4 — 2026-09-19

- **修复自动更新（关键）**：`parse_update_payload` 不认 jiaoben 接口返回的 `desktopSha256` / `downloadSha256`，
  导致**所有版本**的更新检查一律失败（提示「远程更新必须提供合法的 64 位十六进制 SHA256」）——
  这正是长期以来只能手动发版、客户端无法自更新的原因。现已兼容这两个字段名，
  并补单测 `parses_jiaoben_desktop_sha256_field` 与真机探测 `live_update_check_probe`
  （真机结果：`UPDATE_OK up-to-date`，界面日志「已是最新版 v0.9.4 (#904)」）。
- **修复热更新下载目标**：服务端 `app_client_update.desktop_url` 此前写的是 Setup 安装包，
  而客户端逻辑是「下载后替换自身 exe」，会把主程序替换成安装器。发布脚本改为写便携版
  `DiskJanitor-x.y.z.exe`（Setup 仅保留为商城手动下载链接）；接口现返回
  `desktopUrl=.../DiskJanitor-0.9.4.exe`，其 sha 与便携产物一致。
- 新增真机探测测试（均 `#[ignore]`，默认不跑）：`live_status_probe`、`live_redeem_probe`、`live_update_check_probe`。
- 注意：**0.9.4 之前的版本仍无法自动更新**（它们不认识 `desktopSha256`），需手动安装一次 0.9.4；
  从 0.9.4 起后续版本可正常自动更新。

## 0.9.3 — 2026-09-19

- **修复激活/授权链路（关键）**：线上接口已强制「POST + 加密信封」，而客户端一直发明文 JSON，
  导致状态查询和激活一律被拒（`{"code":400,"message":"无效的加密信封"}`）——即**客户拿到卡密也激活不了**。
  - 新增 `src/crypto_transport.rs`，与 run-jane Python 端 `crypto_transport.py` 同协议：
    RSA-OAEP(SHA-256) 包裹随机 AES-256 key + AES-256-GCM 业务体，
    AAD 绑定 `version|keyId|timestamp|requestId|scope`（响应再加 `|response`），
    公钥缓存 + keyId 失效自动刷新并重试一次。
  - `license.rs` 的 `status` / `redeem` / `unbind` 改为走信封（scope `app-license.{action}`），
    旧服务端仍保留一次明文回退。
  - 真机验证：`cargo test --release --bin disk-janitor live_status_probe -- --ignored --nocapture`
    → `PROBE_OK valid=false message=Some("设备未激活")`，说明服务端已能正确解密信封。
- **卡密页重做**：大号圆角蓝色 logo、24px 标题、全宽输入框（浅灰底 + 圆角 + 占位提示）、
  全宽蓝色主按钮、次要操作（刷新状态 / 解绑本机 / 操作文档）居中一行、彩色状态提示条、
  底部分隔线 + 设备数说明与版本号。

## 0.9.2 — 2026-09-19

- **代码签名**：产物改用 Windows Authenticode 签名（新增 `scripts/sign-artifacts.ps1`，证书取当前用户「代码签名」证书，
  可用 `DISKJANITOR_CERT_SHA1` 指定）。`pack.cmd` / `pack-setup.ps1` 已在构建后自动签名；
  没有证书的机器只告警、不阻塞出包。
- **激活页改版**：白卡片 + 蓝色 logo 与主按钮、通信中状态、彩色提示条、底部「同卡最多 3 台设备 / 解绑 / 版本号」说明。
- 修复 `pack.cmd` 未结束带版本号的副本进程（`DiskJanitor-x.y.z.exe`），导致 `copy` 静默失败、
  `app-update.json` 的 SHA256 与实际便携产物不一致（会让热更新校验失败）。
- 已知问题（待处理）：线上授权接口已改为「POST + 加密信封」，客户端仍按旧协议发明文 JSON，
  激活/查询会被服务端拒绝 `{"code":400,"message":"无效的加密信封"}`；需客户端补信封，或服务端为
  `disk-janitor` 增加与 `points-checkin` 同类的 legacy 明文入口。

## 0.9.1 — 2026-09-19

- **界面改版**（对齐「C盘清理助手」类产品）：
  - 左侧导航改为「图标 + 标题 + 一句话说明」的双行样式，选中态为淡蓝底 + 左侧蓝条，品牌区与底部版本信息重排；
  - 总览页新增顶部大号「共发现 X 可清理文件」主视觉与「刷新扫描 / 一键清理」主操作按钮；
  - 磁盘卡片改为环形进度（已使用百分比 + 环内文案）+ 进度条 + 已用/可用/总容量图例 + 文件系统徽标；
  - 新增「可清理项目」卡片列表（图标 + 标题 + 说明 + 大小，点击跳到垃圾建议）；
  - 主题升级：主色由青色改为蓝色 `#2563EB`，浅色外观对齐参考图（浅灰底 + 白卡片 + 大圆角），深色外观同步换色；新增环形进度、列表行、徽标等通用组件。
- **域名切换**：更新与授权地址统一为 `https://jiaoben.lidashuai.top`；
  `rewrite_public_host` 会把历史花生壳域名 `1ph1hf8043323.vicp.fun` 与直连 IP `111.229.202.251[:8687]` 自动重写到该域名。
- 修复 `duplicates.rs` 在非 Windows 目标上的未使用变量告警（`metadata` 仅 unix 分支需要）。

## 0.9.0 — 2026-09-19 发布补充

- 新增**卡密授权门闩**（未激活只显示激活页，支持离线票据缓存）与**残留白名单**（`src/license.rs`、`src/whitelist.rs`）。
- 修复 stable 工具链下无法编译：`src/duplicates.rs` 原使用未稳定的 `windows_by_handle`
  （`volume_serial_number()` / `file_index()`），改为 kernel32 的 `GetFileInformationByHandle`，
  硬链接识别（卷序列号 + 文件索引）语义保持不变，打开失败时回退按路径判定。
- 修复 `updater.rs` 中 `load_json` 无法推断类型导致的编译错误（补 `Self` 标注）。
- 修复 `pack.cmd` / `pack-setup.ps1` 在 Windows PowerShell 5.1 下以 GBK 读取 UTF-8 清单，
  导致 `ConvertFrom-Json` 失败、校验误报「desktopUrl must use HTTPS」。
- 发布 versionCode **900**：安装包与便携版上传服务器，商城下载链接与使用说明页同步更新。
- 已知问题：花生壳域名 `1ph1hf8043323.vicp.fun` 当前映射失效
  （`phtunnel` 日志 `account invalid / overflow`），本轮下载链接走直连 IP
  `http://111.229.202.251`；花生壳恢复后改回脚本里的 `BASE` 重跑即可。
